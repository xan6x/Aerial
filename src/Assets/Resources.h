#pragma once

// Binary assets embedded into the DLL so the client stays a single file to
// inject. Ids are shared between Resources.rc and the C++ that loads them.

#define AERIAL_ASSET_ASUKA   101
#define AERIAL_ASSET_AYANAMI 102
